import { boxIcon, searchIcon } from "../assets/icons/index";

export const IconMap: Record<string, string> = {
    SEARCH: searchIcon,
    BOX: boxIcon
}

export enum Icons {
    SEARCH = 'SEARCH',
    BOX = 'BOX'
}